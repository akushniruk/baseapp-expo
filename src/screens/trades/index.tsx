import React from "react";
import { View, Text } from "react-native";

const TradesScreen = () => {
    return (
        <View>
            <Text>Trades</Text>
        </View>
    );
};

export default TradesScreen;
